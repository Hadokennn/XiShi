# 复制为 .env 后填实际值。.env 已在 .gitignore，不会被提交。

# Anthropic（主力）
ANTHROPIC_API_KEY=sk-ant-xxx

# 国产 LLM（仅 experiments/llm_provider_diff.py 学习对比用，主路径不依赖）
DEEPSEEK_API_KEY=sk-xxx
MOONSHOT_API_KEY=sk-xxx

# Postgres
DATABASE_URL=postgresql://localhost:5432/xishi
